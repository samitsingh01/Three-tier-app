apiVersion: v1
kind: Service
metadata:
  name: todo-backend
spec:
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 31222        # Service port
      targetPort: 5000  # Container port
  type: clusterIP      # Internal only (use NodePort/LoadBalancer if exposing outside)
