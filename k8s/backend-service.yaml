apiVersion: v1
kind: Service
metadata:
  name: todo-backend
spec:
  selector:
    app: todo-backend
  ports:
    - protocol: TCP
      port: 5000        # Service port
      targetPort: 5000  # Container port
      nodeport: 31222
  type: NodePort      # Internal only (use NodePort/LoadBalancer if exposing outside)
